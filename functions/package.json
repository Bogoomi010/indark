{
  "name": "indark-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "lint": "eslint .",
    "serve": "npm run build && npx firebase-tools emulators:start --only functions,firestore",
    "deploy": "npm run build && npx firebase-tools deploy --only functions,firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/seedrandom": "^3.0.8"
  }
}
